<h2><%= @patient.name %></h2>
<p>
  Age: <%= @patient.age %>
</p>
Appointments:
  <% @patient.appointments.each do |appt| %>
    <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> at
    <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
  <% end %>